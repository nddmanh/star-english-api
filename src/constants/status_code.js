const SUCCESS = 200;

const BAD_REQUEST = 400;

const UNAUTHORIZED = 401;

const FORBIDDEN = 403;

const NOT_FOUND = 404;

const SERVER_ERROR_INTERNAL = 500;

module.exports = {
  SUCCESS,
  BAD_REQUEST,
  UNAUTHORIZED,
  FORBIDDEN,
  NOT_FOUND,
  SERVER_ERROR_INTERNAL
};
